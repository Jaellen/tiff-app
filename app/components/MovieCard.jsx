var React = require('react');

var MovieCard = React.createClass({
  render: function () {
    return (
      <div>
        Title
        Image
      </div>
    );
  }
});

module.exports = MovieCard;
